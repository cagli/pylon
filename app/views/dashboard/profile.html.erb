<div class="row">

	<h1> Profile</h1>

    <h1> <%= current_user.username%></h1> 
    <br>   
    <%= current_user.first_name + " " + current_user.last_name%>
    <br>
    <%= current_user.email %>
    <%= button_to "Editer Profile", @article, remote: true %>

  <%= render "devise/registrations/edit" %>

</div>
 
